# Project Brief: rails_hmvc Gem

## 1. Core Goal

- **Build the `rails_hmvc` gem** to standardize the structure of Rails projects according to the HMVC architecture.
- **Provide a CLI** with commands for initialization and automatic resource generation (controllers, operations, forms, routes).
